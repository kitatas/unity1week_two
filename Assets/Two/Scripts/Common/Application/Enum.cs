namespace Two.Common.Application
{
    public enum SceneName
    {
        Title,
        Main,
    }

    public enum BgmType
    {
        Title,
        Main,
        Result,
    }

    public enum SeType
    {
        Decision,
        Cancel,
        Damage,
        Matched,
        Alert,
    }

    public enum ButtonType
    {
        Decision,
        Cancel,
    }
}